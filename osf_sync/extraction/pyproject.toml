[project]
name = "pdf_extraction"
version = "0.1.0"
description = "Dependencies for extraction helpers and author extraction workflows."
readme = "../README.md"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.0.2",
    "pypdf>=6.1.2",
    "boto3>=1.34",
    "requests>=2.31",
]
